<%= form_for(@bicicletum) do |f| %>
  <% if @bicicletum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bicicletum.errors.count, "error") %> prohibited this bicicletum from being saved:</h2>

      <ul>
      <% @bicicletum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :marca %><br>
    <%= f.text_field :marca %>
  </div>
  <div class="field">
    <%= f.label :precio %><br>
    <%= f.text_field :precio %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
